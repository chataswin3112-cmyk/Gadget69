# Frontend-only Vite envs. Backend datasource uses `DATABASE_URL` on Render
# or `SPRING_DATASOURCE_URL`, `SPRING_DATASOURCE_USERNAME`,
# `SPRING_DATASOURCE_PASSWORD` for manual setups.
VITE_API_BASE_URL=/api
